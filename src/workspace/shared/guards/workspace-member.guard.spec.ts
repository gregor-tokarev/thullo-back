import { WorkspaceMemberGuard } from './workspace-member.guard';

describe('WorkspaceMemberGuard', () => {
  it('should be defined', () => {
    expect(new WorkspaceMemberGuard()).toBeDefined();
  });
});
